import {resolveBookmarkController} from '../bootstrap';
import resolveServiceManager from '../services/ServiceManager';

export async function chooseBookmarkGutter() {
  const sm = resolveServiceManager();
}
